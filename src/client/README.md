# src/client
Client runtime and code generation support.

- `runtime.ts`: Base class and transport/types used by generated clients.
- `generator/`: Per-file parser/emitter/orchestration for generated client classes.
