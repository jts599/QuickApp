# src/http
Transport-layer abstractions and Express integration.

- `types.ts`: Framework HTTP request/response types.
- `expressAdapter.ts`: Express -> framework handler adapter.
- `expressAutoRoutes.ts`: Auto route registration for RPC endpoints.
