# scripts
Repository-level helper scripts.

- `copy-migration-assets.mjs`: Copies migration SQL files into `dist/` after build.
- Client generation CLI lives at `src/client/generator/cli.ts` and is run via `npm run generate:client`.
